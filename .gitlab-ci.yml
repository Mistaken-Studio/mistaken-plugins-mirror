stages:
  - prebuild
  - build

download-builder:
  stage: prebuild
  tags:
    - Windows
  script:
    - 'curl -L --ssl-no-revoke --header "PRIVATE-TOKEN: a1YCfdhji5bgscqCxFmr" "https://git.mistaken.pl/api/v4/projects/Barwa/CI-Tools/jobs/artifacts/main/download?job=build-builder"'

build-plugins:
  needs: [download-builder]
  stage: build
  tags:
    - Windows
  script:
    - '"CI Builder.exe" %CI_COMMIT_REF_NAME% %CI_COMMIT_SHA% %CI_COMMIT_REF_NAME% true'
  artifacts:
    paths:
      - plugins.tar.gz 
